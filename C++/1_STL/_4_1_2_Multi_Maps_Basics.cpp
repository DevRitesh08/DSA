// THE only benefit of it is that it can store duplicate elements with same keys , but still we can skip this by using -->
// map<int,vector<int> > or unordered_map<int,vector<int> >
// so,
//Ignore multi-map !!!!